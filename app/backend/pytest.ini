[pytest]
# Default: API tests only. ``ingestion`` also defines ``services``; run both via
# ``python run_backend_tests.py`` (or ``pytest tests/ingestion`` in a fresh shell).
testpaths = tests/api
pythonpath = api
addopts = --import-mode=prepend
filterwarnings =
    ignore::DeprecationWarning
