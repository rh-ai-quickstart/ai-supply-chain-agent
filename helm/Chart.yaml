apiVersion: v2
name: supply-chain-agent
description: AI Supply Chain Agent — demo dashboard via general-simulation platform subchart
type: application
version: 0.2.0
appVersion: "0.1.0"

dependencies:
  # Postgres, Neo4j, Llama Stack, llm-service, API, and ingestion are owned by
  # general-simulation — packaged from the git submodule (vendor/).
  #
  # Published chart: https://rh-ai-quickstart.github.io/general-simulation
  # Submodule tracks general-simulation development; run `make submodule-init` after clone.
  - name: general-simulation
    version: 1.0.1
    repository: file://../vendor/general-simulation/deploy/helm
