apiVersion: v2
name: supply-chain-agent
description: AI Supply Chain Agent — demo dashboard via general-simulation platform subchart
type: application
version: 0.2.0
appVersion: "0.1.0"

dependencies:
  # Postgres, Neo4j, Llama Stack, llm-service, API, and ingestion are owned by
  # general-simulation 0.0.1 — not sibling dependencies of this umbrella.
  - name: general-simulation
    version: 0.0.1
    repository: https://robertsandoval.github.io/general-simulation
