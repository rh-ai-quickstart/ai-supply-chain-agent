apiVersion: v2
name: supply-chain-agent
description: AI Supply Chain Agent — demo dashboard via general-simulation platform subchart
type: application
version: 0.2.0
appVersion: "0.1.0"

dependencies:
  # Postgres, Neo4j, Llama Stack, llm-service, API, and ingestion are owned by
  # general-simulation — not sibling dependencies of this umbrella.
  #
  # Published chart: https://rh-ai-quickstart.github.io/general-simulation
  # Local dev override: make helm-deps-local (packages sibling checkout into charts/)
  - name: general-simulation
    version: 1.0.0
    repository: https://rh-ai-quickstart.github.io/general-simulation
